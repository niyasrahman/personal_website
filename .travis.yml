language: node_js
node_js:
  - "8"
cache: yarn
branches:
  only:
    - landing
before_script:
  - "yarn global add firebase-tools"
  - "yarn global add gatsby"
script:
  - "yarn test"
  - "yarn build:prod"
after_success:
  - "firebase deploy --token=${KEY}"
